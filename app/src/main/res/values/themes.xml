<resources>
    <!-- Base application theme. -->
    <style name="Theme.Go4Lunch" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryVariant">@color/red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your theme here. -->
<!--        <item name="android:colorPrimary">@color/red</item>
        <item name="android:colorPrimaryDark">@color/red_dark</item>
-->
    </style>
    <!-- Base application theme. -->
    <style name="Theme.Go4Lunch2" parent="Theme.Go4Lunch">
        <!-- Primary brand color. -->
        <item name="android:background">@color/red</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
<!--        <item name="android:textColorSecondary">@color/white</item>  -->
    </style>
</resources>